.. _configuration-file:

Legacy TOML configuration
=========================

TOML configuration files define integrated coarsening and history-matching studies for the predefined Norne and Drogon models.

.. warning::

   This workflow is retained for legacy, model-specific studies. Current development targets generic OPM Flow deck input. New users should start with :doc:`tutorial` and :doc:`command-line`.

Only ``-i``, ``-o``, ``-f``, and ``-precision`` apply on the command line when a TOML file is selected.

.. toctree::
   :maxdepth: 1

   configuration/complete-reference
